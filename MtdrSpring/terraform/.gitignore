# Generated by CI (utils/ci/terraform-apply.sh) — never commit
backend_ci_override.tf

# Terraform local state / working files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log

# ONNX embedding model cache (downloaded by embeddings.tf, ~80 MB)
.cache/
